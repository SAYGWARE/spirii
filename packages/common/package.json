{
  "name": "common",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "build": "tsup ./src/index.ts --format cjs --dts",
    "dev": "npm run build -- --watch --silent"
  },
  "devDependencies": {
    "@swc/core": "^1.6.13",
    "tsup": "^8.1.1"
  }
}
